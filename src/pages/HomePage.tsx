import { DefaultLayout } from "../layouts";
import { FormFormik } from "../components/FormFormik/index";

export const HomePage = () => {
  return (
    <DefaultLayout>
      <FormFormik />
    </DefaultLayout>
  );
};
